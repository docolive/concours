{% extends 'base.html.twig' %}

{% block title %}Gestion des concours{% endblock %}

{% block body %}
<a href="{{path('concours_add')}}"><button class="btn btn-success">Créer un concours</button></a>

<table class="table table-striped table-borderd">
<tr>
<th>NOM</th>
<th>Actions</th>
</tr>
{% for concours in concourses %}
    <tr>
    <td>{{concours.name}}</td>
    <td>
    <a href="{{ path('concours_edit', {'id': concours.id}) }}">
    <button class="btn btn-primary btn-sm py-0" style="font-size: 0.8em;"><i class="fas fa-edit"></i></button></a>
    {% include "concours/_delete_form.html.twig" %}
    </td>
    </tr>
{% endfor %}
</table>
{% endblock %}
