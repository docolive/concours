{% extends "base.html.twig" %}
{% block body %}
<div class="text-center">
<a href="{{path('dashboard')}}"><button class="btn btn-primary">OK</button></a>
</div>

<div class="buttons d-flex justify-content-around">
<div>
<a href="{{path('app_send_verify_email')}}"><button class="btn btn-success">Envoyer à nouveau le mail à {{mail}}</button></a>
</div>

<div>
<a href="{{path('edit_email')}}"><button class="btn btn-success">Modifier mon adresse mail</button></a>
</div>
</div>
{% endblock %}