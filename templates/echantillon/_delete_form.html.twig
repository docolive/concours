<form method="post" action="{{ path('echantillon_delete', {'id': echantillon.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet échantillon ?');" style="display:inline-block">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ echantillon.id) }}">
    <button class="btn btn-danger btn-sm py-0" style="font-size: 0.8em;"><i class="fas fa-trash-alt"></i></button></a>
</form>
