<div class="row mt-4 d-flex justify-content-center">
<div class="col-md-6">
{{ form_start(form) }}
    {{ form_widget(form) }}
    
{{ form_end(form) }}
<button class="btn btn-success mt-4" id="enregistreur">{{ button_label|default('Enregistrer') }}</button>
</div>
</div>