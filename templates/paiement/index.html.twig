{% extends 'base.html.twig' %}

{% block title %}Gestion des modes de paiements{% endblock %}

{% block body %}
<a href="{{path('paiement_add')}}"><button class="btn btn-success">Créer un mode de paiement</button></a>

<table class="table table-striped table-bordered">
<tr>
<th>Mode de paiement</th>
<th>Actions</th>
</tr>
{% for p in paiements %}
    <tr>
    <td>{{p.name}}</td>
    <td>
    <a href="{{ path('paiement_edit', {'id': p.id}) }}">
    <button class="btn btn-primary btn-sm py-0" style="font-size: 0.8em;"><i class="fas fa-edit"></i></button></a>
    {% include "paiement/_delete_form.html.twig" %}
    </td>
    </tr>
{% endfor %}
</table>
{% endblock %}
